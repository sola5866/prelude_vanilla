# AGENTS.md

## リポジトリ概要

このリポジトリは Minecraft Bedrock Edition の Resource Pack アドオンを管理するモノレポです。

`addons/*` の各ディレクトリが、独立して配布可能な1つのパックを表します。

## ディレクトリ構成

### addons

Codexは `addons/*` をアドオンの追加・変更対象として扱ってください。

### dist

ビルド成果物の出力先です。生成物は `dist/` 配下へ出力してください。

### .github/workflows

GitHub Actions の設定です。ビルド処理を変更する場合は GitHub Actions との整合性を維持してください。

## 正本と確認順序

変更対象に応じて、次の仕様と実装を確認する。

* ビルド仕様: `docs/build/`
* 検証仕様: `docs/validation/`
* リリース仕様: `docs/release/`
* 実際のCI条件: `.github/workflows/`

文書と実装が異なる場合は、差異と影響を報告してから修正する。

## 設計原則

### アドオンは独立している

各アドオンは単独でビルド・配布可能とし、他アドオンへの依存を追加しない。

### 配布物の互換性を維持する

既存アドオンのフォルダ構成や Minecraft が参照するパスを変更しない。

リファクタリング時は配布物の内容が変化しないことを優先する。

### ビルドは自動検出を優先する

アドオン名をハードコーディングせず、`addons/*` を走査してビルド対象を検出する。

## Codexへの指示

### 変更前に分析する

大規模な変更を行う前に、影響範囲、修正対象ファイル、想定リスクを確認する。

### 次の場合は確認を求める

* ディレクトリ移動
* 名前変更
* ビルド方式変更
* GitHub Actions の全面的な書き換え
* 既存アドオンへの互換性影響

### 避けること

* 不要なリネーム
* 不要な整形変更
* ファイル内容の一括書き換え
* アドオンの仕様変更

### 機密情報

`.env` は読み取らない。設定項目の確認には `.env.example` を使用する。

## 検証

通常の変更後は次を実行する。

```bash
uv run python tools/validate/validate.py
```

配布内容またはビルド処理に影響する変更後は、検証に加えて全アドオンをビルドする。

```bash
uv run python tools/build/build-all.py --version 99.99.99-test
```

検証を実行できなかった場合は、成功扱いにせず理由と未確認範囲を報告する。

## 完了条件

変更後は影響範囲に応じて以下を満たすこと。

* 通常の変更では、GitHub Actions と同じ検証コマンドがローカルで成功する
* 配布内容またはビルド処理に影響する変更では、全アドオンをビルドして `dist/` に成果物を出力できる
* 配布物の内容が意図せず変化しない
* 新しいアドオンを `addons/` 配下へ追加しても自動検出できる

GitHub Actions 自体を実行していない場合は、その状態を未確認として報告する。

## 文字コード

ドキュメントとデータファイルは UTF-8 で統一する。
Markdown と JSON は UTF-8 で読み書きする。
ASCII だけで足りる場面でも、既存の文書やデータが UTF-8 を使っている場合はそれに合わせる。
